{% if content %}
	<div{{attributes.addClass('contain-main-menu')}}>
		<nav class="container navbar navbar-expand-lg navbar-light">
			<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>
			{% if content.layout_builder_add_block %}
			<div {{ region_attributes.site_main_menu }}>
			    {{ content.site_main_menu }}
			</div>
			{% else %}
			<div {{region_attributes.site_main_menu.addClass('collapse','navbar-collapse','site_main_menu')}} id="navbarSupportedContent">
				{{ content.site_main_menu }}
			</div>
			{% endif %}
		</nav>
	</div>
{%endif %}